<Window x:Class="NetworkAnalysisApp.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Traffic Statistics" Height="400" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <GroupBox Header="Total Packets" Margin="0,0,15,0" Padding="10">
                <TextBlock Text="{Binding TotalPackets}" FontSize="24" FontWeight="Bold" Foreground="DarkBlue"/>
            </GroupBox>
            <GroupBox Header="Total Bytes" Padding="10">
                <TextBlock Text="{Binding TotalBytes}" FontSize="24" FontWeight="Bold" Foreground="DarkGreen"/>
            </GroupBox>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Protocol Breakdown">
            <DataGrid ItemsSource="{Binding ProtocolStats}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="100"/>
                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="100"/>
                    <DataGridTextColumn Header="Bytes" Binding="{Binding Bytes}" Width="100"/>
                    
                    <DataGridTemplateColumn Header="Distribution" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding Percentage}" Height="15" Margin="5,0"/>
                                    <TextBlock Text="{Binding Percentage, StringFormat={}{0:N1}%}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>
